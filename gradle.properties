org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
# Committed on purpose — the keystore trade-off is a locked v0.2 decision: signature
# stability for a personal, sideload-only project. See README "Release installs";
# rotate to CI secrets if distribution ever changes.
RELEASE_STORE_PASSWORD=dumb-switch-release
